<label className="block font-semibold text-gray-700">
  Profesor:
  <select
    name="Profesor"
    value={formData.Profesor?.toString()}
    className="w-full border p-2 rounded-lg focus:ring-2 focus:ring-[#990000] outline-none"
    onChange={(e) =>
      setFormData((prev) => ({
        ...prev,
        Profesor: parseInt(e.target.value),
      }))
    }
    required
  >
    <option value="">Selecciona una opción</option>
    {profesores.map((profesor) => (
      <option key={profesor.id_emp} value={profesor.id_emp.toString()}>
        {profesor.nombre}
      </option>
    ))}
  </select>
</label>

<label className="block font-semibold text-gray-700">
  Segundo Profesor:
  <select
    name="SegundoPro"
    value={formData.SegundoPro?.toString()}
    className="w-full border p-2 rounded-lg focus:ring-2 focus:ring-[#990000] outline-none"
    onChange={(e) =>
      setFormData((prev) => ({
        ...prev,
        SegundoPro: parseInt(e.target.value),
      }))
    }
    required
  >
    <option value="">Selecciona una opción</option>
    {profesores.map((profesor) => (
      <option key={profesor.id_emp} value={profesor.id_emp.toString()}>
        {profesor.nombre}
      </option>
    ))}
  </select>
</label>