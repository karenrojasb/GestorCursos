const usuario = await prisma.usuario.findUnique({
  where: { id_emp },
  select: { nombre: true, publico: true },
});

if (!usuario) {
  throw new Error("Usuario no encontrado.");
}

return { nombre: usuario.nombre, publico: usuario.publico };