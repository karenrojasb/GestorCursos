const handleChange = (e: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement>) => {
  if (!editandoCurso) return;
  const { name, value } = e.target;

  // Convertir valores de `select` a número si es necesario
  const camposNumericos = ["Valor", "Publico", "Horas", "Linea", "Estado", "Modalidad", "IdTipoCurso"];
  
  setEditandoCurso((prev) => {
    if (!prev) return null;
    return {
      ...prev,
      [name]: camposNumericos.includes(name) ? parseInt(value) || 0 : value,
    };
  });
};