const handleFiltroPublico = (event: React.ChangeEvent<HTMLSelectElement>) => {
  const valor = parseInt(event.target.value);

  // Si el valor es 0 o null, mostrar todos los cursos
  setFiltroPublico(valor === 0 ? null : valor);

  const cursosFiltrados = cursos.filter(curso => {
    if (valor === 0 || valor === null) return true; // Muestra todos los cursos si no hay filtro
    return Number(curso.Publico) === valor; // Filtra según el valor seleccionado
  });

  setCursosFiltrados(cursosFiltrados);
};




<select onChange={handleFiltroPublico}>
  <option value={0}>Todos</option>
  {publicos.map((pub) => (
    <option key={pub.id} value={pub.id}>
      {pub.Especificacion}
    </option>
  ))}
</select>